interface:
  display_name: "全流程创作超级智能体"
  short_description: "隔离项目、Prompt V2、原生 image-2 与完整创作管线"
  brand_color: "#1F6F63"
  default_prompt: "Use $creator-super-agent to create an isolated project, compile Prompt V2, generate with Codex native image-2, persist outputs, and execute the full production pipeline."
policy:
  allow_implicit_invocation: true
